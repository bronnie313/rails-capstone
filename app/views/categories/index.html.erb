<nav id="nav" class="navbar navbar-expand-sm bg-primary navbar-dark">
  <div class="nav-list">
     <i class="fa-solid fa-bars"></i>
    <h1>Categories</h1>
    <%= link_to "Logout", destroy_user_session_path, method: :delete, class: 'btn btn-outline-dark btn-sm mt-2 float-start' %></br>
  </div>
</nav>
<p style="color: green"><%= notice %></p>
<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>
    <p>
      <%= link_to "Show this category", category %>
    </p>
  <% end %>
</div>

<%= link_to "New category", new_category_path, class: 'btn btn-outline-dark btn-sm mt-2 float-start' %>
