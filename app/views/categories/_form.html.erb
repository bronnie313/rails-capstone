<div class="category-form">
<%= form_with(model: category, html: { class: 'transaction-form' }) do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class: 'input-form' %>
  </div>

  <div>
    <%= form.label :icon, style: "display: block" %>
    <%= form.text_field :icon, class: 'input-form' %>
  </div>

  <div>
    <%= form.submit class: "btn btn-outline-dark btn-sm float-start" %>
  </div>
<% end %>
</div>
